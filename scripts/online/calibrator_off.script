# This script turn off the calibrator light
# Require a CoverCalibrator driver

from ccdciel import ccdciel;
import sys

resp = ccdciel('CALIBRATOR_LIGHT_OFF')['result']['status']
if resp!='OK!':
   sys.exit(1)

txt = ccdciel('calibratorstatus')['result']
ccdciel('logMsg','Calibrator is '+txt)

